mutation AddRoutine($input: RoutineInput!) {
    addRoutine(input: $input) {
        routine {
          name
          exercises {
            name
            sets {
              kg
              reps
            }
          }
        }
        errors {
            message
        }
    }
}
