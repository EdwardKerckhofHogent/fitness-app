mutation register($input: UserInput!) {
    register(input: $input) {
        user {
            id
            email
        }
        errors {
            path
            message
        }
    }
}
